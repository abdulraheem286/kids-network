import { Collapse } from 'antd'
import React from 'react'
const { Panel } = Collapse
const Products = () => {
    return (
        <Collapse>
            <Panel>
                Poduct
            </Panel>
        </Collapse>
    )
}

export default Products